#include "main.h"

/**
 * print_alphabet - print the alphabet in lower case
 * Return: 0 (Success)
 */

int main (void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
